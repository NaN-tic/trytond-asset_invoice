<?xml version="1.0"?>
<!-- The COPYRIGHT file at the top level of this repository contains the full
     copyright notices and license terms. -->
<tryton>
    <data>
        <record model="ir.ui.view" id="invoice_line_view_form">
            <field name="model">account.invoice.line</field>
            <field name="inherit" ref="account_invoice.invoice_line_view_form"/>
            <field name="name">invoice_line_form</field>
            <field name="priority" eval="12"/>
        </record>

        <!-- asset to invoice -->
        <record model="ir.action.act_window" id="act_asset_invoice_form2">
            <field name="name">Invoices</field>
            <field name="res_model">account.invoice</field>
            <field name="domain"
                eval="[If(Eval('active_ids', []) == [Eval('active_id')], ('invoice_asset', '=', Eval('active_id')), ('invoice_asset', 'in', Eval('active_ids')))]"
                pyson="1"/>
        </record>
        <record model="ir.action.keyword" id="act_open_asset_invoice_keyword1">
            <field name="keyword">form_relate</field>
            <field name="model">asset,-1</field>
            <field name="action" ref="act_asset_invoice_form2"/>
        </record>
        <record model="ir.action-res.group" id="act_invoice_form2-group_invoice">
            <field name="action" ref="act_asset_invoice_form2"/>
            <field name="group" ref="account.group_account"/>
        </record>
    </data>
</tryton>
